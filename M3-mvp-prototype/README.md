# Модуль M3. ПРОТОТИПИРОВАНИЕ MVP

**Сроки:** Недели 5–6  
**Вес модуля:** 34 баллов  
**Форма аттестации:** защита работы на приёмке (разговор с командой)  
**Компетенции КРМ:** LLM-1, LLM-5, PL-1 — индикаторы LLM-1.2, LLM-5.2, PL-1.1, PL-1.2 ([сопоставление](../docs/competency-map.md))

## Задача модуля

Первый рабочий прототип: API отвечает, база хранит, baseline-модель генерирует, интерфейс показывает — и evals уже меряют, насколько это хорошо.

## Контрольно-измерительные материалы

### Product / Vision Owner

Вес роли в модуле: 12 из 34.

| Код | Что проверяется | Как проверяется | Балл |
|---|---|---|---:|
| `lab3-error-report` | Отчёт «где врём» содержит типы ошибок с примерами и приоритетом по боли | экспертная оценка по тексту работы | 3 |
| `lab3-uc-validated` | Каждый UC прогнан на прототипе, отклонения записаны | экспертная оценка по тексту работы | 3 |
| `lab3-requirements-v2` | Требования v2 обновлены явными правками | экспертная оценка по тексту работы | 2 |
| `lab3-e2e` | E2E-прогон пройден, разрывы — задачи с владельцами | экспертная оценка по тексту работы | 2 |
| `lab3-next-sprint` | План следующего спринта с ценами техдолга | экспертная оценка по тексту работы | 2 |

**Вопросы на приёмке.** Задаются вживую, ответ студента оценивает преподаватель:

- Спроси, какая ошибка из отчёта злит пользователя сильнее всего и почему она первая в списке.
- Спроси, какой UC на прототипе повёл себя не так, как было написано.
- Спроси, какой техдолг взяли сознательно и что он будет стоить в лабе 5.

### AI Engineer

Вес роли в модуле: 6 из 34.

| Код | Что проверяется | Как проверяется | Балл |
|---|---|---|---:|
| `lab3-tracing` | Каждый LLM-вызов виден в трассировке | экспертная оценка по тексту работы | 2 |
| `lab3-experiments-reproducible` | Прогон экспериментов воспроизводится, latency и токены выгружены в репозиторий | экспертная оценка по тексту работы | 2 |
| `lab3-baseline-meaning` | На приёмке объяснено, что цифры baseline значат для продукта | разговор на приёмке | 2 |

**Вопросы на приёмке.** Задаются вживую, ответ студента оценивает преподаватель:

- Спроси: эта цифра — это хорошо или плохо для вашего пользователя? Почему?

### Delivery Engineer

Вес роли в модуле: 9 из 34.

| Код | Что проверяется | Как проверяется | Балл |
|---|---|---|---:|
| `lab3-compose-up` | docker compose up поднимает всю систему одной командой | запуск команды | 3 |
| `lab3-prod-url` | Прототип доступен на публичном прод-URL | живой URL | 3 |
| `lab3-chain` | UI вызывает API, API вызывает модель, результаты в БД | экспертная оценка по тексту работы | 3 |

**Вопросы на приёмке.** Задаются вживую, ответ студента оценивает преподаватель:

- Попроси провести один запрос от кнопки в UI до записи в БД и назвать, где он может застрять.

### AI Quality & Safety

Вес роли в модуле: 7 из 34.

| Код | Что проверяется | Как проверяется | Балл |
|---|---|---|---:|
| `lab3-evals-baseline` | Evals прогнаны на baseline по golden dataset | структура репозитория | 3 |
| `lab3-evals-ci` | Мини-набор evals гоняется в CI с порогом | структура репозитория | 2 |
| `lab3-metrics-counted` | Метрики из карты качества посчитаны фактически | экспертная оценка по тексту работы | 2 |

## О формате контрольно-измерительных материалов

Отдельных файлов `kim-*.md` и `rubric-*.md` в модуле нет: КИМ здесь —
таблица выше, и она же рубрика.

Причина в том, что оценивание бинарное по каждому критерию: артефакт либо
удовлетворяет описанию, либо нет. Уровней «частично» не предусмотрено — их
нельзя проверить автоматически и вокруг них возникает торг. Градация
появляется на другом уровне: из ста четырёх критериев закрывается больше
или меньше.

Второй слой — приёмка. Она булевая и служит допуском: не принято — оценки
за модуль нет, сколько бы критериев ни закрылось. Вопросы к приёмке
приведены выше по ролям.
