# Модуль M4. PRODUCTION-READY ИНФРАСТРУКТУРА

**Сроки:** Недели 7–10  
**Вес модуля:** 45 баллов  
**Форма аттестации:** защита работы на приёмке (разговор с командой)  
**Компетенции КРМ:** LC-4, LC-5, LLM-1 — индикаторы LC-4.1, LC-4.3, LC-5.1, LLM-1.5 ([сопоставление](../docs/competency-map.md))

## Задача модуля

Прототип становится системой, которой можно доверять: CI/CD деплоит сам, мониторинг видит всё, evals ловят регрессии, инъекции отбиты, а скоуп под контролем burndown.

## Контрольно-измерительные материалы

### Product / Vision Owner

Вес роли в модуле: 8 из 45.

| Код | Что проверяется | Как проверяется | Балл |
|---|---|---|---:|
| `lab4-burndown` | Burndown вёлся весь спринт и лежит в репо | экспертная оценка по тексту работы | 2 |
| `lab4-mvp-scope` | MVP-скоуп зафиксирован, вырезанное — с причинами | экспертная оценка по тексту работы | 3 |
| `lab4-acceptance` | Еженедельные приёмки запротоколированы | экспертная оценка по тексту работы | 2 |
| `lab4-requirements-v3` | Требования v3 обновлены | экспертная оценка по тексту работы | 1 |

**Вопросы на приёмке.** Задаются вживую, ответ студента оценивает преподаватель:

- Спроси, в какой момент спринта стало понятно, что не успеваете, и что сделали.
- Спроси, что вырезали из MVP и по какой причине.

### AI Engineer

Вес роли в модуле: 13 из 45.

| Код | Что проверяется | Как проверяется | Балл |
|---|---|---|---:|
| `lab4-ab-code` | A/B-сценарии живут в коде, результаты прогонов выгружены в репозиторий | экспертная оценка по тексту работы | 2 |
| `lab4-ab-decision` | На приёмке объяснено, какой вариант выбрали по цифрам A/B и почему | разговор на приёмке | 2 |
| `lab4-prompts-versioned` | Промпты живут как версионируемые артефакты, каждая итерация подтверждена прогоном | экспертная оценка по тексту работы | 3 |
| `lab4-cost` | Стоимость сценария посчитана до и после оптимизации | экспертная оценка по тексту работы | 2 |
| `lab4-tool-contract` | Инструмент модели вызывается по контракту, его отказ обработан | экспертная оценка по тексту работы | 2 |
| `lab4-retrieval-measured` | Если ответ строится на найденных документах, у поиска есть отдельный замер | экспертная оценка по тексту работы | 2 |

**Вопросы на приёмке.** Задаются вживую, ответ студента оценивает преподаватель:

- Спроси, какой вариант выиграл и на сколько — в цифрах, а не «лучше».
- Спроси, чем промпт v2 отличается от v1 и что показал прогон до и после.
- Спроси, сколько стоит один сценарий сейчас и сколько стоил месяц назад.
- Спроси, что произойдёт, если инструмент ответит ошибкой или не ответит вовсе.
- Спроси: поиск не нашёл нужный документ или модель выдумала поверх найденного — и откуда это известно.

### Delivery Engineer

Вес роли в модуле: 13 из 45.

| Код | Что проверяется | Как проверяется | Балл |
|---|---|---|---:|
| `lab4-cd` | Мёрж в main автоматически деплоит на прод | структура репозитория | 3 |
| `lab4-staging` | Staging поднимается из тех же compose-файлов | экспертная оценка по тексту работы | 2 |
| `lab4-dashboard` | Дашборд показывает метрики, логи и трейсы системы | экспертная оценка по тексту работы | 3 |
| `lab4-token-limits` | Лимиты токенов работают: аномалия вызывает алерт | экспертная оценка по тексту работы | 2 |
| `lab4-model-rights` | Права модели ограничены кодом: необратимое действие проходит через приложение | экспертная оценка по тексту работы | 3 |

**Вопросы на приёмке.** Задаются вживую, ответ студента оценивает преподаватель:

- Попроси показать на дашборде, что происходило с системой вчера вечером.
- Спроси, какое необратимое действие есть в продукте и кто решает, что его можно выполнить.

### AI Quality & Safety

Вес роли в модуле: 11 из 45.

| Код | Что проверяется | Как проверяется | Балл |
|---|---|---|---:|
| `lab4-full-evals` | Полный eval-набор гоняется в CI с порогами | структура репозитория | 3 |
| `lab4-attacks` | Прогон атак выполнен набором сценариев, дыры оформлены задачами | экспертная оценка по тексту работы | 3 |
| `lab4-integration-tests` | Критические UC покрыты интеграционными тестами | экспертная оценка по тексту работы | 2 |
| `lab4-attacks-closed` | Пробившиеся атаки закрыты, закрытие подтверждено повторным прогоном | экспертная оценка по тексту работы | 3 |

**Вопросы на приёмке.** Задаются вживую, ответ студента оценивает преподаватель:

- Спроси, какая атака прошла и что изменили, чтобы система её выдержала.
- Попроси прогнать сценарий, который раньше проходил, и показать, что теперь он не проходит.

## О формате контрольно-измерительных материалов

Отдельных файлов `kim-*.md` и `rubric-*.md` в модуле нет: КИМ здесь —
таблица выше, и она же рубрика.

Причина в том, что оценивание бинарное по каждому критерию: артефакт либо
удовлетворяет описанию, либо нет. Уровней «частично» не предусмотрено — их
нельзя проверить автоматически и вокруг них возникает торг. Градация
появляется на другом уровне: из ста четырёх критериев закрывается больше
или меньше.

Второй слой — приёмка. Она булевая и служит допуском: не принято — оценки
за модуль нет, сколько бы критериев ни закрылось. Вопросы к приёмке
приведены выше по ролям.
